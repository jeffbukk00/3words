const WriteBoardWritingFooter = (props) => {
    const { children } = props;
    return <footer className="write-board-writing-footer">{children}</footer>;
};

export default WriteBoardWritingFooter;
